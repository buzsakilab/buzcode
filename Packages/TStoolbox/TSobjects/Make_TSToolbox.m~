function Make_TSToolbox()

objDirs = dir('@*');
dirNames = {'@intervalSet';'@tsd'};

for ii=1:length(dirNames{ii})
   cfiles = dir(
   if exist(objDir(ii).name,'folder')
       cd(
   mex cfiles(ii).name
end
